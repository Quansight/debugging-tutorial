# Profiling NumPy with samply

*Stub: this tutorial hasn't been written yet.*

It will use [samply](https://github.com/mstange/samply) to profile NumPy
and inspect the results in the Firefox Profiler. Like `../gdb-tutorial/`, it
will ship a self-contained `pixi.toml` and `pixi.lock`.
